/** SearchForm shows search bar
 *
 * Props:
 * - searchFunction (function to call upon submit)
 * 
 * State:
 * - controlled form
 *
 * { CompanyList, JobList } -> SearchForm
 */

 function SearchForm(){
  return <header>SEARCH FORM</header>;
}

export default SearchForm;